services:
  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: openrag-prometheus
    ports:
      # Localhost only: the Prometheus API is unauthenticated.
      - "127.0.0.1:9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    configs:
      # The scrape bearer, read by prometheus.yml (credentials_file). Written
      # straight from METRICS_TOKEN so the API and its scraper can't disagree.
      - source: openrag_metrics_token
        target: /etc/prometheus/openrag_token
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=30d"
      # --web.enable-lifecycle is intentionally NOT set: it exposes unauthenticated
      # /-/reload and /-/quit endpoints. Re-enable only behind an auth proxy.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  grafana:
    image: grafana/grafana:11.2.2
    container_name: openrag-grafana
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?Set GRAFANA_ADMIN_PASSWORD in .env}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=${GF_SERVER_ROOT_URL:-http://localhost:3000}
      - GF_SERVER_SERVE_FROM_SUB_PATH=${GF_SERVER_SERVE_FROM_SUB_PATH:-false}
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      # The chart's copy is the only one: Helm's .Files.Glob reads only inside
      # the chart directory, and the Kubernetes ConfigMaps are rendered from it.
      - ../charts/openrag-stack/dashboards:/var/lib/grafana/dashboards:ro
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    restart: unless-stopped

  node-exporter:
    image: prom/node-exporter:v1.8.2
    container_name: openrag-node-exporter
    ports:
      # Localhost only: Prometheus scrapes it over the compose network by name,
      # so it needn't be exposed on all interfaces (it surfaces host metrics).
      - "127.0.0.1:9100:9100"
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--path.rootfs=/rootfs"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"
    restart: unless-stopped

  nvidia-gpu-exporter:
    image: utkuozdemir/nvidia_gpu_exporter:1.13.1
    container_name: openrag-nvidia-gpu-exporter
    ports:
      # Localhost only: scraped over the compose network by name.
      - "127.0.0.1:9835:9835"
    environment:
      - NVIDIA_DRIVER_CAPABILITIES=utility
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped

  # The API runs Ray embedded, and Ray picks a random port for its metrics
  # agent unless told otherwise. Pin it so the "ray" scrape job can name it:
  # indexing and the embed/VLM calls are recorded in Ray actors and only ever
  # reach Prometheus through that agent. Unauthenticated, so it is reachable on
  # the compose network only and deliberately absent from `ports:`. Both API
  # variants share the `openrag` alias the job targets.
  openrag:
    environment:
      RAY_METRICS_EXPORT_PORT: "8091"

  openrag-cpu:
    environment:
      RAY_METRICS_EXPORT_PORT: "8091"

configs:
  # Inline `content` needs Docker Compose >= 2.23.1. GET /metrics fails closed
  # without METRICS_TOKEN, so the overlay refuses to start rather than scrape
  # a 403 forever. Generate one with `openssl rand -hex 16`.
  openrag_metrics_token:
    content: "${METRICS_TOKEN:?Set METRICS_TOKEN in .env — the monitoring overlay sends it as the bearer on GET /metrics}"

volumes:
  prometheus_data:
  grafana_data:
